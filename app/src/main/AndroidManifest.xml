<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.monster.smokeproject">

    <uses-feature android:name="android.hardware.camera"
        android:required="true" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="18"/>
    <uses-permission android:name="android.permission.CAMERA"
        android:maxSdkVersion="16"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ValidarPlacaActivity"
            android:label="@string/title_activity_validar"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".VistoriaActivity"
            android:label="@string/title_activity_validar"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".NovaVistoriaActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".ApoioActivity" />
        <activity android:name=".VistoriaRealizadaActivity" />
        <activity android:name=".DetailsActivity" />

    </application>

</manifest>